[project]
name = "earnings-oracle"
version = "0.1.0"
description = "Volatility-based earnings options trading strategies"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
